{
  "extends": "./tsconfig.json",
  "preset": "ts-jest",
  "isolatedModules": true,
  "testEnvironment": "node",
  "compilerOptions": {
    "jsx": "react-jsx",
    "types": ["node", "jest"] // Add "jest" here
  }
}
