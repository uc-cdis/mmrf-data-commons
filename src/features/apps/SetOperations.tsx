import React, { FC } from "react";

import SetOperationsSelection from "../set-operations/SetOperationsSelection";

const SetOperations: FC = () => {
  return <SetOperationsSelection />;
};

export default SetOperations;
