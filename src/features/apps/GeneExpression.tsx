import React from "react";
import { MatrixWrapper } from "../proteinpaint/MatrixWrapper";

const GeneExpressionApp: React.FC = () => {
  return <MatrixWrapper chartType="hierCluster" />;
};

export default GeneExpressionApp;
